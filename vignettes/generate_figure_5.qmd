---
title: "Figure 5"
format: html
---

```{r}
library(ggplot2)
library(CensusHLA)
```


# Prep work

Pull information on state codes and names from a package-stored function


```{r}
(state_info <- query_state_codes())
```

```{r, fig.height = 8, fig.width = 8}
ca_francisco <- make_county_census_tract_allele_frequency_map(
  state_abbreviation = 'CA',
  query_allele = 'A*11:01',
  county_code = '075',
  county_name = 'San Francisco County'
)
ca_francisco
#ggsave("fig_5_sf.svg", ca_francisco$p1, device = "svg")
```

# System and Session info

```{r}
sessionInfo()
Sys.info()
```


